import React, { useEffect, useMemo, useState } from 'react';
import { CalendarDays, Check, ChevronLeft, ChevronRight, CircleHelp, Clock3, Filter, Flag, Folder, Inbox, Menu, MoreHorizontal, Plus, Search, Settings, Sparkles, Tag, Target, Trash2, X, SlidersHorizontal, RefreshCw, Headphones, Paperclip, Pin, CheckCircle2, Repeat2, ListTodo } from 'lucide-react';
import { createSubtask, deleteSubtask, loadSubtasks, updateSubtask } from './backend/subtaskRepository';
import { supabaseEnabled } from './backend/supabase';
import './styles.css';
import './history.css';
import './next7.css';

const pad=n=>String(n).padStart(2,'0');
const key=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
const day0=d=>new Date(d.getFullYear(),d.getMonth(),d.getDate());
const plus=(d,n)=>{const x=new Date(d);x.setDate(x.getDate()+n);return x;};
const iso=()=>new Date().toISOString();
const dateFor=t=>{if(t.dueAt){const d=new Date(t.dueAt);if(!Number.isNaN(d.getTime()))return key(d);}if(/^\d{4}-\d{2}-\d{2}$/.test(t.date||''))return t.date;const d=day0(new Date());if(t.date==='today')return key(d);if(t.date==='tomorrow')return key(plus(d,1));return null;};
const normalize=(t,i=0)=>{const createdAt=t.createdAt||new Date(Date.now()-i*1000).toISOString();return {...t,id:t.id??`${Date.now()}-${i}`,title:t.title||'Untitled task',date:t.date||'upcoming',time:t.time||'',list:t.list||'Personal',tag:t.tag||'',priority:t.priority||'none',pinned:!!t.pinned,done:!!t.done,notes:t.notes||'',createdAt,updatedAt:t.updatedAt||createdAt,completedAt:t.completedAt||null,reminderAt:t.reminderAt||null,recurrenceRule:t.recurrenceRule||''};};
const initial=[
 {id:1,title:'To-do app completion',date:'today',list:'Personal'},
 {id:2,title:'check all the projects to be deployed',date:'today',list:'Personal'},
 {id:3,title:'New System design now',date:'today',list:'Personal'},
 {id:4,title:'AWS Revision',date:'today',list:'Personal'},
 {id:5,title:'AWS new course 2 section',date:'today',list:'Personal'},
 {id:6,title:'DSA',date:'today',list:'Personal'},
 {id:7,title:'Review tomorrow interview plan',date:'tomorrow',list:'Personal'}
].map(normalize);

export default function CompleteApp(){
 const [tasks,setTasks]=useState(()=>{try{const x=JSON.parse(localStorage.getItem('anyday.tasks')||'null');return Array.isArray(x)?x.map(normalize):initial;}catch{return initial;}});
 const [view,setView]=useState('myday'),[selected,setSelected]=useState(null),[search,setSearch]=useState(''),[dark,setDark]=useState(true),[menu,setMenu]=useState(false),[settings,setSettings]=useState(false),[filterOpen,setFilterOpen]=useState(false),[status,setStatus]=useState('active'),[priority,setPriority]=useState('all'),[lists,setLists]=useState(()=>{try{return JSON.parse(localStorage.getItem('anyday.lists')||'null')||['Personal','Work'];}catch{return ['Personal','Work'];}}),[tags,setTags]=useState(()=>{try{return JSON.parse(localStorage.getItem('anyday.tags')||'null')||['Important','Work','Personal','Health'];}catch{return ['Important','Work','Personal','Health'];}}),[newList,setNewList]=useState(''),[newTag,setNewTag]=useState('');
 useEffect(()=>localStorage.setItem('anyday.tasks',JSON.stringify(tasks)),[tasks]);
 useEffect(()=>localStorage.setItem('anyday.lists',JSON.stringify(lists)),[lists]);
 useEffect(()=>localStorage.setItem('anyday.tags',JSON.stringify(tags)),[tags]);
 const matches=useMemo(()=>tasks.filter(t=>(!search||t.title.toLowerCase().includes(search.toLowerCase()))&&(priority==='all'||t.priority===priority)),[tasks,search,priority]);
 const today=day0(new Date()),todayKey=key(today);
 const active=matches.filter(t=>!t.done), selectedTask=tasks.find(t=>String(t.id)===String(selected))||null;
 const update=(id,patch)=>setTasks(v=>v.map(t=>String(t.id)===String(id)?{...t,...patch,updatedAt:iso()}:t));
 const toggle=id=>setTasks(v=>v.map(t=>String(t.id)===String(id)?{...t,done:!t.done,completedAt:!t.done?iso():null,updatedAt:iso()}:t));
 const remove=id=>{setTasks(v=>v.filter(t=>String(t.id)!==String(id)));setSelected(null);};
 const add=(title,date=today)=>{if(!title.trim())return;const ts=iso(),t=normalize({id:`${Date.now()}-${Math.random().toString(36).slice(2)}`,title:title.trim(),date:key(date),list:'Personal',createdAt:ts,updatedAt:ts});setTasks(v=>[t,...v]);return t.id;};
 const myDay=active.filter(t=>dateFor(t)===todayKey);
 const next7Count=active.filter(t=>{const k=dateFor(t);return k&&k>=todayKey&&k<=key(plus(today,6));}).length;
 const clearCompleted=()=>setTasks(v=>v.filter(t=>!t.done));
 const createNamedList=()=>{const n=newList.trim();if(n&&!lists.includes(n))setLists(v=>[...v,n]);setNewList('');};
 const createNamedTag=()=>{const n=newTag.trim();if(n&&!tags.includes(n))setTags(v=>[...v,n]);setNewTag('');};
 return <div className={dark?'app dark':'app'}>
  <aside className={menu?'sidebar mobile-open':'sidebar'}><div className="account"><div className="gear-circle"><Settings size={22}/></div><div><strong>Sree</strong><span>All features enabled</span></div></div><button className="primary-add" onClick={()=>document.querySelector('.quick-add input')?.focus()}><Plus size={18}/> Add task</button>
   <div className="side-scroll"><div className="nav-group">{[['myday','My day',Target,myDay.length],['next7','Next 7 days',CalendarDays,next7Count],['all','All my tasks',Inbox,matches.length],['calendar','My Calendar',CalendarDays,'']].map(([id,label,I,count])=><button key={id} className={view===id?'nav active':'nav'} onClick={()=>{setView(id);setMenu(false);}}><I size={19}/><span>{label}</span>{count!==''&&<em>{count}</em>}</button>)}</div>
   <div className="side-heading"><span>My lists</span><button onClick={()=>{const n=prompt('List name');if(n?.trim()&&!lists.includes(n.trim()))setLists(v=>[...v,n.trim()]);}}><Plus size={19}/></button></div>{lists.map((x,i)=><button className="nav list-nav" key={x} onClick={()=>setView('list:'+x)}><span className={'list-dot dot-'+(i%2)}></span><span>{x}</span><em>{active.filter(t=>t.list===x).length}</em></button>)}
   <div className="side-heading"><span>Tags</span><button onClick={()=>{const n=prompt('Tag name');if(n?.trim()&&!tags.includes(n.trim()))setTags(v=>[...v,n.trim()]);}}><Plus size={19}/></button></div>{tags.map(x=><button className="nav tag-nav" key={x} onClick={()=>setView('tag:'+x)}><Tag size={15}/><span>{x}</span></button>)}</div>
   <div className="side-bottom"><button className="nav"><CircleHelp size={19}/> Help & feedback</button><button className="nav" onClick={()=>setSettings(true)}><Settings size={19}/> Settings</button></div></aside>
  <main className="workspace"><header className="topbar"><button className="mobile-menu" onClick={()=>setMenu(v=>!v)}><Menu size={22}/></button><div className="brand-small">AnyDay</div><div className="top-icons"><div className="global-search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search tasks"/></div><button title="Sync"><RefreshCw size={19}/></button><button title="Help"><Headphones size={20}/></button><button title="Notifications"><Inbox size={20}/></button><button onClick={()=>setDark(v=>!v)} title="Theme">◐</button></div></header>
   <div className="main-grid"><section className="day-panel">
    {view==='myday'&&<MyDay tasks={myDay} today={today} toggle={toggle} select={setSelected} add={add}/>} 
    {view==='next7'&&<Next7 tasks={matches} today={today} toggle={toggle} select={setSelected} add={add} filterOpen={filterOpen} setFilterOpen={setFilterOpen} status={status} setStatus={setStatus} priority={priority} setPriority={setPriority} clearCompleted={clearCompleted}/>} 
    {view==='calendar'&&<CalendarView tasks={matches} today={today} select={setSelected}/>} 
    {(view==='all'||view.startsWith('list:')||view.startsWith('tag:'))&&<TaskCollection view={view} tasks={matches} today={today} status={status} setStatus={setStatus} toggle={toggle} select={setSelected} add={add} clearCompleted={clearCompleted}/>} 
   </section><aside className="right-panel"><div className="right-tools"><button className="suggestions-title"><Sparkles size={19}/> Suggestions</button><button onClick={()=>setFilterOpen(v=>!v)}><SlidersHorizontal size={19}/></button><button onClick={()=>setSettings(true)}><Settings size={19}/></button></div><div className="filter-row"><span>Filter</span><button onClick={()=>setFilterOpen(v=>!v)}><Filter size={17}/></button></div>{filterOpen&&<div className="filter-menu"><button onClick={()=>setPriority('all')}>All priorities</button><button onClick={()=>setPriority('high')}>High priority</button><button onClick={()=>setPriority('medium')}>Medium priority</button><button onClick={()=>setPriority('low')}>Low priority</button></div>}<div className="suggestion-stack"><div className="suggestion-card"><button className="plus-suggest"><Plus size={19}/></button><div><div className="suggestion-source">⌑ My lists &gt; Personal</div><strong>Watch My day tutorial</strong><small>Recently active</small></div></div><div className="suggestion-card"><button className="plus-suggest"><Plus size={19}/></button><div><div className="suggestion-source">⌑ My lists &gt; Personal</div><strong>Add me to My Day</strong><small>Suggested</small></div></div></div><div className="tutorial-card"><div className="fake-video"><div className="play">▶</div></div><strong>Tap to learn about My day</strong></div></aside></div></main>
  {selectedTask&&<TaskDetail task={selectedTask} update={update} remove={remove} close={()=>setSelected(null)} lists={lists} tags={tags}/>} {settings&&<SettingsModal close={()=>setSettings(false)} dark={dark} setDark={setDark} lists={lists} tags={tags} newList={newList} setNewList={setNewList} createList={createNamedList} newTag={newTag} setNewTag={setNewTag} createTag={createNamedTag}/>} 
 </div>;
}

function MyDay({tasks,today,toggle,select,add}){const [v,setV]=useState('');const submit=e=>{e.preventDefault();if(v.trim()){add(v,today);setV('');}};return <div className="myday-shell"><div className="hero-copy"><h1>Good Afternoon, Sree<span>.</span></h1><h2>You are what you do</h2></div><div className="calendar-connect"><div className="date-block"><b>{today.toLocaleDateString('en-US',{weekday:'short'}).toUpperCase()}</b><strong>{today.getDate()}</strong><span>{today.toLocaleDateString('en-US',{month:'long'})}</span></div><div className="connect-copy"><strong>Join video meetings with one tap</strong><div><button>📅 Connect Google Calendar</button><button>▣ Connect Outlook Calendar</button><button>☁ Connect iCloud Calendar</button></div></div></div><TaskList tasks={tasks} toggle={toggle} select={select}/><form className="quick-add" onSubmit={submit}><Plus size={20}/><input value={v} onChange={e=>setV(e.target.value)} placeholder="Enter task title"/><button>↑</button></form></div>}
function TaskList({tasks,toggle,select}){return <div className="task-list">{tasks.length?tasks.map(t=><TaskRow key={t.id} task={t} toggle={toggle} select={select}/>):<div className="empty-state">Your day is clear. Add a task below.</div>}</div>}
function TaskRow({task,toggle,select}){return <div className={'task-row '+(task.done?'completed':'')} onClick={()=>select(task.id)}><button className="circle-check" onClick={e=>{e.stopPropagation();toggle(task.id)}}>{task.done&&<Check size={14}/>}</button><div className="task-text"><div>{task.title}</div>{(task.time||task.tag||task.priority!=='none')&&<small>{task.time}{task.tag&&` · ${task.tag}`}{task.priority!=='none'&&` · ${task.priority}`}</small>}</div><div className="task-end">{task.pinned&&<Pin size={13}/>}<MoreHorizontal size={20}/></div></div>}

function Next7({tasks,today,toggle,select,add,filterOpen,setFilterOpen,status,setStatus,priority,setPriority,clearCompleted}){const [anchor,setAnchor]=useState(day0(today));const [inputs,setInputs]=useState({});const days=Array.from({length:7},(_,i)=>plus(anchor,i));const filtered=tasks.filter(t=>{const k=dateFor(t);if(!k||k<key(anchor)||k>key(plus(anchor,6)))return false;if(status==='active'&&t.done)return false;if(status==='completed'&&!t.done)return false;return true;});const submit=(d,e)=>{e.preventDefault();const v=inputs[key(d)]||'';if(v.trim()){add(v,d);setInputs(x=>({...x,[key(d)]:''}));}};return <div className="next7-view"><div className="next7-toolbar"><div className="next7-title-pill"><CalendarDays size={22}/><strong>Next 7 days</strong></div><div className="next7-toolbar-actions"><button onClick={()=>setFilterOpen(v=>!v)}><SlidersHorizontal size={18}/> Filter</button><button className="next7-more"><MoreHorizontal size={20}/></button><button className="clear-completed" onClick={clearCompleted}><CheckCircle2 size={17}/> Clear completed</button></div></div>{filterOpen&&<div className="next7-filter-popover"><div><span>Status</span><button className={status==='active'?'selected':''} onClick={()=>setStatus('active')}>Active</button><button className={status==='all'?'selected':''} onClick={()=>setStatus('all')}>All</button><button className={status==='completed'?'selected':''} onClick={()=>setStatus('completed')}>Completed</button></div><div><span>Priority</span><button className={priority==='all'?'selected':''} onClick={()=>setPriority('all')}>All</button><button className={priority==='high'?'selected':''} onClick={()=>setPriority('high')}>High</button><button className={priority==='medium'?'selected':''} onClick={()=>setPriority('medium')}>Medium</button></div></div>}<div className="next7-scroller"><div className="next7-board">{days.map((d,i)=>{const k=key(d),day=filtered.filter(t=>dateFor(t)===k).sort((a,b)=>Number(b.pinned)-Number(a.pinned)||new Date(a.createdAt)-new Date(b.createdAt));const todayHere=k===key(today);const label=i===0&&todayHere?'Today':i===1&&todayHere?'Tomorrow':d.toLocaleDateString('en-US',{weekday:'long'});return <section className={'next7-day-card '+(todayHere?'is-today':'')} key={k}><header><div><strong>{label}</strong><span>{d.toLocaleDateString('en-US',{weekday:'short'})}</span></div><small>{d.toLocaleDateString('en-US',{month:'short',day:'numeric'})}</small></header><div className="next7-task-stack">{day.map(t=><Next7Task key={t.id} task={t} toggle={toggle} select={select}/>)}{!day.length&&<div className="next7-empty">No tasks</div>}</div><form className="next7-inline-add" onSubmit={e=>submit(d,e)}><Plus size={18}/><input value={inputs[k]||''} onChange={e=>setInputs(x=>({...x,[k]:e.target.value}))} placeholder="Add Task"/></form></section>})}</div></div><div className="next7-footer"><button onClick={()=>setAnchor(plus(anchor,-7))}><ChevronLeft size={17}/> Previous 7 days</button><button className="next7-today" onClick={()=>setAnchor(day0(today))}>Today</button><span>{filtered.length} task{filtered.length===1?'':'s'} shown</span><button onClick={()=>setAnchor(plus(anchor,7))}>Next 7 days <ChevronRight size={17}/></button></div></div>}
function Next7Task({task,toggle,select}){return <article className={'next7-task '+(task.done?'done':'')} onClick={()=>select(task.id)}><button className="next7-check" onClick={e=>{e.stopPropagation();toggle(task.id)}}>{task.done&&<Check size={13}/>}</button><div className="next7-task-content"><div className="next7-source"><span>♙</span> My lists &gt; {task.list||'Personal'}</div><strong>{task.title}</strong>{(task.time||task.tag||task.priority!=='none')&&<small>{task.time}{task.tag&&` · ${task.tag}`}{task.priority!=='none'&&` · ${task.priority}`}</small>}</div>{task.pinned&&<Pin size={14} className="next7-pin"/>}</article>}

function TaskCollection({view,tasks,today,status,setStatus,toggle,select,add,clearCompleted}){const title=view==='all'?'All my tasks':view.startsWith('list:')?view.slice(5):view.slice(4);let list=tasks;if(view.startsWith('list:'))list=tasks.filter(t=>t.list===title);if(view.startsWith('tag:'))list=tasks.filter(t=>t.tag===title);if(status==='active')list=list.filter(t=>!t.done);if(status==='completed')list=list.filter(t=>t.done);const grouped=list.reduce((a,t)=>{const k=(t.createdAt||iso()).slice(0,10);(a[k]||=[]).push(t);return a},{});return <div className="generic-history"><div className="generic-head"><div><h1>{title}</h1><p>{list.length} task{list.length===1?'':'s'} · complete history retained</p></div><button onClick={()=>setStatus(v=>v==='all'?'active':'all')}><Filter size={17}/></button></div><div className="history-controls"><div className="history-control-group"><button className={status==='all'?'selected':''} onClick={()=>setStatus('all')}>All</button><button className={status==='active'?'selected':''} onClick={()=>setStatus('active')}>Active</button><button className={status==='completed'?'selected':''} onClick={()=>setStatus('completed')}>Completed</button></div>{view==='all'&&<button className="clear-history" onClick={clearCompleted}>Clear completed</button>}</div>{Object.entries(grouped).sort(([a],[b])=>b.localeCompare(a)).map(([k,items])=><section className="history-group" key={k}><div className="history-heading"><span>{new Date(`${k}T12:00:00`).toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric',year:'numeric'})}</span><em>{items.length}</em></div>{items.map(t=><TaskRow key={t.id} task={t} toggle={toggle} select={select}/>)}</section>)}{!list.length&&<div className="history-empty"><ListTodo size={34}/><h3>No matching tasks</h3><p>Add a task or change the filter.</p></div>}<QuickCollectionAdd add={add} today={today}/></div>}
function QuickCollectionAdd({add,today}){const [v,setV]=useState('');return <form className="quick-add" onSubmit={e=>{e.preventDefault();if(v.trim()){add(v,today);setV('')}}}><Plus size={20}/><input value={v} onChange={e=>setV(e.target.value)} placeholder="Enter task title"/><button>↑</button></form>}

function CalendarView({tasks,today,select}){const [month,setMonth]=useState(new Date(today.getFullYear(),today.getMonth(),1));const first=new Date(month.getFullYear(),month.getMonth(),1);const start=(first.getDay()+6)%7;const count=new Date(month.getFullYear(),month.getMonth()+1,0).getDate();const cells=Array.from({length:Math.ceil((start+count)/7)*7},(_,i)=>{const n=i-start+1;return n>0&&n<=count?n:null});return <div className="calendar-view"><div className="generic-head"><div><h1>My Calendar</h1><p>Tasks with due dates, month by month.</p></div><div className="month-nav"><button onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))}><ChevronLeft size={17}/></button><strong>{month.toLocaleString('en-US',{month:'long',year:'numeric'})}</strong><button onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))}><ChevronRight size={17}/></button></div></div><div className="calendar-grid">{['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map(x=><div className="calendar-day-name" key={x}>{x}</div>)}{cells.map((n,i)=>{const d=n?new Date(month.getFullYear(),month.getMonth(),n):null;const k=d?key(d):'';const day=tasks.filter(t=>dateFor(t)===k);return <div className={'calendar-cell '+(k===key(today)?'today':'')} key={i}>{n&&<b>{n}</b>}{day.slice(0,3).map(t=><button className="calendar-task" key={t.id} onClick={()=>select(t.id)}>{t.title}</button>)}</div>})}</div></div>}

function TaskDetail({task,update,remove,close,lists,tags}){const [title,setTitle]=useState(task.title);const [note,setNote]=useState(task.notes||'');const [subtasks,setSubtasks]=useState([]);const [subTitle,setSubTitle]=useState('');const [reminder,setReminder]=useState(task.reminderAt?task.reminderAt.slice(0,16):'');useEffect(()=>{let live=true;loadSubtasks(task.id).then(x=>{if(live)setSubtasks(x)}).catch(()=>{});return()=>{live=false}},[task.id]);useEffect(()=>{setTitle(task.title);setNote(task.notes||'');setReminder(task.reminderAt?task.reminderAt.slice(0,16):'')},[task.id,task.title,task.notes,task.reminderAt]);const saveTitle=()=>{if(title.trim()&&title!==task.title)update(task.id,{title:title.trim()})};const saveNote=()=>{if(note!==task.notes)update(task.id,{notes:note})};const addSub=async()=>{if(!subTitle.trim())return;const temp={id:`local-${Date.now()}`,task_id:task.id,title:subTitle.trim(),completed:false,position:subtasks.length};setSubtasks(v=>[...v,temp]);setSubTitle('');try{const row=await createSubtask(task.id,temp.title);if(row)setSubtasks(v=>v.map(x=>x.id===temp.id?row:x))}catch{}};const toggleSub=async s=>{const next=!s.completed;setSubtasks(v=>v.map(x=>x.id===s.id?{...x,completed:next}:x));if(!String(s.id).startsWith('local-'))try{await updateSubtask(s.id,{completed:next})}catch{}};const delSub=async s=>{setSubtasks(v=>v.filter(x=>x.id!==s.id));if(!String(s.id).startsWith('local-'))try{await deleteSubtask(s.id)}catch{}};return <aside className="detail-panel"><div className="detail-top"><button onClick={close}><ChevronLeft size={16}/></button><div><button onClick={()=>update(task.id,{pinned:!task.pinned})}><Pin size={14}/> {task.pinned?'Unpin':'Pin'}</button><button onClick={()=>update(task.id,{priority:task.priority==='high'?'none':'high'})}><Flag size={14}/> Priority</button></div><button onClick={()=>remove(task.id)}><Trash2 size={17}/></button></div><input className="detail-title" value={title} onChange={e=>setTitle(e.target.value)} onBlur={saveTitle} onKeyDown={e=>e.key==='Enter'&&e.currentTarget.blur()}/><DetailSection label="LIST"><select className="detail-select" value={task.list||'Personal'} onChange={e=>update(task.id,{list:e.target.value})}>{lists.map(x=><option key={x}>{x}</option>)}</select></DetailSection><DetailSection label="DATE"><div className="date-options"><button className={task.date==='today'?'chosen':''} onClick={()=>update(task.id,{date:'today',dueAt:null})}>Today</button><button className={task.date==='tomorrow'?'chosen':''} onClick={()=>update(task.id,{date:'tomorrow',dueAt:null})}>Tomorrow</button><input type="date" value={/^\d{4}-\d{2}-\d{2}$/.test(task.date||'')?task.date:''} onChange={e=>update(task.id,{date:e.target.value,dueAt:e.target.value||null})}/></div></DetailSection><DetailSection label="REMINDER"><div className="reminder-editor"><Clock3 size={15}/><input type="datetime-local" value={reminder} onChange={e=>{setReminder(e.target.value);update(task.id,{reminderAt:e.target.value?new Date(e.target.value).toISOString():null})}}/><button onClick={()=>{setReminder('');update(task.id,{reminderAt:null})}}>Clear</button></div></DetailSection><DetailSection label="REPEAT"><div className="repeat-row"><Repeat2 size={15}/><select value={task.recurrenceRule||''} onChange={e=>update(task.id,{recurrenceRule:e.target.value})}><option value="">Does not repeat</option><option value="daily">Every day</option><option value="weekly">Every week</option><option value="monthly">Every month</option></select></div></DetailSection><DetailSection label="SUBTASKS"><div className="sub-add"><Plus size={15}/><input value={subTitle} onChange={e=>setSubTitle(e.target.value)} onKeyDown={e=>e.key==='Enter'&&addSub()} placeholder="Add a subtask"/></div>{subtasks.map(s=><div className="sub-item" key={s.id}><button className="circle-check" onClick={()=>toggleSub(s)}>{s.completed&&<Check size={12}/>}</button><span className={s.completed?'sub-done':''}>{s.title}</span><button className="sub-delete" onClick={()=>delSub(s)}><X size={13}/></button></div>)}</DetailSection><DetailSection label="NOTES"><textarea value={note} onChange={e=>setNote(e.target.value)} onBlur={saveNote} placeholder="Add notes…"/></DetailSection><DetailSection label="TAG"><select className="detail-select" value={task.tag||''} onChange={e=>update(task.id,{tag:e.target.value})}><option value="">No tag</option>{tags.map(x=><option key={x}>{x}</option>)}</select></DetailSection><DetailSection label="ATTACHMENTS"><div className="attachment"><Paperclip size={14}/> Attachments are ready for backend storage</div></DetailSection></aside>}
function DetailSection({label,children}){return <div className="detail-section"><div className="detail-label">{label}</div>{children}</div>}
function SettingsModal({close,dark,setDark,lists,tags,newList,setNewList,createList,newTag,setNewTag,createTag}){return <div className="overlay" onMouseDown={e=>e.target===e.currentTarget&&close()}><div className="settings-modal"><div className="settings-head"><div><h2>Settings</h2><p>Customize your AnyDay workspace.</p></div><button onClick={close}><X size={19}/></button></div><div className="settings-row"><div><strong>Dark theme</strong><span>Use the dark workspace appearance.</span></div><button className="toggle" onClick={()=>setDark(v=>!v)}>{dark?'On':'Off'}</button></div><div className="settings-row"><div><strong>Lists</strong><span>{lists.length} lists available.</span></div><form onSubmit={e=>{e.preventDefault();createList()}}><input value={newList} onChange={e=>setNewList(e.target.value)} placeholder="New list"/><button><Plus size={16}/></button></form></div><div className="settings-row"><div><strong>Tags</strong><span>{tags.length} tags available.</span></div><form onSubmit={e=>{e.preventDefault();createTag()}}><input value={newTag} onChange={e=>setNewTag(e.target.value)} placeholder="New tag"/><button><Plus size={16}/></button></form></div><div className="settings-row"><div><strong>Notifications</strong><span>Browser reminders require notification permission.</span></div><button onClick={()=>typeof Notification!=='undefined'&&Notification.requestPermission()}>Enable</button></div></div></div>}
